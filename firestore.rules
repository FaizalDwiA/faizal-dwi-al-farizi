rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Keamanan tinggi: Hanya Firebase Admin SDK (migrate.js) yang bisa menulis karena mem-bypass rules
    }
  }
}
